query login($email: String!, $password: String!) {
    login(email: $email, password: $password) {
        user {
            _id
            bewoner{
                _id
                name
                description
                moveInDate
                moveOutDate
                profilePicture
            }
        }
        token
        tokenExpiration
    }
}
